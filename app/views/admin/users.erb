<table class="table">
  <tr>
    <th>#</th>
    <th>Username</th>
    <th>Date</th>
  </tr>
  <% @users.each do |user| %>
  <tr>
    <td><%= user.id %></td>
    <td><%= user.username %></td>
    <td><%= user.created_at %></td>
  </tr>
  <% end %>
</table>

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
  <!-- Content Header (Page header) -->
  <section class="content-header">
    <h1>
      Users
      <!-- <small>add, modify, or delete users</small> -->
    </h1>
    <ol class="breadcrumb">
      <li><a href="/admin"><i class="fa fa-dashboard"></i> Dashboard</a></li>
      <li><a href="/admin/users">Users</a></li>
    </ol>
  </section>

  <!-- Main content -->
  <section class="content">
    <div class="row">
      <div class="col-md-12">
        <div class="box">
          <div class="box-header with-border pull-left">
            <a href="/admin/user" class="btn-sm btn btn-default">Add New +</a>
          </div><!-- /.box-header -->
          <div style="clear:both;">
          <div class="box-body">

            <table class="table table-bordered table-hover datatable">
              <thead>
                <tr>
                  <th style="width: 10px">#</th>
                  <th>Username</th>
                  <th>Email</th>
                  <th>Role</th>
                  <th>Created</th>
                  <th style="width: 120px">Actions</th>
                </tr>
              </thead>

              <% @users.each do |user| %>
              <tr id="row_<%= user[:id] %>">
                <td><%= user[:id] %></td>
                <td><%= user[:username] %></td>
                <td><%= user[:email] %></td>
                <td><%= user[:role] %></td>
                <td><%= user[:created_at] %></td>
                <td>
                  <a href="/admin/user/<%=user[:id] %>" class="btn btn-default btn-sm">Edit</a>
                  <a href="/admin/user/delete/<%=user[:id] %>" data-remote="true" data-confirm="Are You Sure?" class="btn bg-warning btn-sm">Delete</a>
                </td>
              </tr>
              <% end %>

            </table>
          </div><!-- /.box-body -->

        </div><!-- /.box -->
      </div><!-- /.col -->


    </div>
  </section><!-- /.content -->
</div><!-- /.content-wrapper -->

<script>
 $(function(){

   $('.datatable').DataTable({
     "paging": true,
     "lengthChange": true,
     "searching": true,
     "ordering": true,
     "order": [[ 0, "desc" ]],
     "info": true,
     "autoWidth": true
   });

 });
</script>
